- name: Restart timesyncd
  ansible.builtin.service:
    name: systemd-timesyncd
    state: restarted

- name: Restart SSH
  ansible.builtin.service:
    name: "{{ ssh_service }}"
    state: restarted
